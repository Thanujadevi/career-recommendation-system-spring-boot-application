package com.example.career_recommendation;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class CareerRecommendationApplicationTests {

    @Test
    void contextLoads() {
        // Verifies that the entire Spring application context starts without errors.
        // This catches: missing beans, bad config, circular dependencies, etc.
    }
}
